version: '3'

services:
  app:
    image: pgbouncersample/app
    ports:
      - 80:80
    environment:
      - UploadUrl=Uploads
      - BackendRoot=https://localhost
    networks:
      - pgbouncersample

  app-postgres-db:
    image: pgbouncersample/postgre-sql
    volumes:
      - DB:/var/lib/pgsql/data/
    ports:
      - 5432:5432
    networks:
      - pgbouncersample

  liquibase:
    image: pgbouncersample/liquibase
    tty: true
    environment:
      - READYCHECK_URLS=http://app app-postgres-db:5432
      - READYCHECK_TIMEOUT=3
      - POSTGRES_USER=flexberryuser
      - POSTGRES_PASSWORD=jhv
      - POSTGRES_HOST=app-postgres-db
      - POSTGRES_PORT=5432
      - POSTGRES_DB=appdb auditdb securitydb
      - SCHEMANAME=liquibase
    volumes:
      - script:/liquibase/sql
    depends_on:
      - app-postgres-db
      - app
    networks:
      - pgbouncersample

  pgbouncer:
    image: pgbouncersample/pgbouncer
    depends_on:
      - app-postgres-db
    ports:
      - 6432:6432
    networks:
      - pgbouncersample

volumes:
  DB:
  script:

networks:
  pgbouncersample:
